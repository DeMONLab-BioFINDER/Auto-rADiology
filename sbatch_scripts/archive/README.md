# Archived SLURM scripts

These predate the current pipeline (`submit_mse-cv5.sh`, `submit_mse-final-47.sh`,
`submit_val_AVID_unseen.sh` in the parent directory) — different model (`UNet3D`),
different datasets (IDEAS/ADNI), hyperparameter tuning, and few-shot fine-tuning,
none of which are part of the active workflow. Kept for reference only.
